FROM node:20-alpine

WORKDIR /app

# Copy package files first
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the project
COPY . .

# Expose Vite's default port
EXPOSE 5173

# Run dev server
# --host is required to expose the server outside the container
CMD ["npm", "run", "dev", "--", "--host"]